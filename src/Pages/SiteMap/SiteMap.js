import React from 'react';
import PropTypes from 'prop-types';
import './SiteMap.css';

const SiteMap = () => (
  <div className="SiteMap" data-testid="SiteMap">
    SiteMap Component
  </div>
);

SiteMap.propTypes = {};

SiteMap.defaultProps = {};

export default SiteMap;
